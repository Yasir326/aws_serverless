# AWS SERVERLESS APP SENECA

Make sure you have [Serverless](https://www.serverless.com/) installed by running ```yarn add serverless```
The functions can be deployed on to aws by using ```sls deploy```
